spring:
  application:
    name: airport-geo-service

# h2 Database Configuration
  datasource:
    url: jdbc:h2:mem:airport-geo-db
    username: diallocode
    password: passer
    driver-class-name: org.h2.Driver
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    show-sql: true
  h2:
    console:
      enabled: true

server:
  port: 8080
