
spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/diallocodes/config-files.git
          clone-on-start: true
          search-paths: development
          default-label: master


  application:
    name: config-server

server:
  port: 8888






